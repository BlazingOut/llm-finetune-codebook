# 一、对话模板

目前主流的对话模型（Instruct/Chat 版本）普遍采用一种**结构化列表**来管理对话上下文，这就是你看到的 `role`（角色）与 `content`（内容）模板。

这种设计不仅是为了代码的可读性，更是为了符合大模型在**指令微调**（Instruction Tuning）阶段的学习模式。

## 1. 核心构成：角色（Roles）的物理定义

在对话列表中，每一个字典代表一轮交谈中的一个片段。通常包含以下三种核心角色：

| **角色 (Role)**   | **定义与功能**                                 | **物理意义**                          |
| --------------- | ----------------------------------------- | --------------------------------- |
| **`system`**    | **全局指令**。设定 AI 的身份、语气、行为准则（如“你是一个幽默的导游”）。 | 相当于给模型运行环境设置“全局变量”，影响后续所有生成的风格。   |
| **`user`**      | **用户输入**。即人类向模型提出的问题或指令。                  | 模型需要响应的“触发信号”，通常是推理的起点。           |
| **`assistant`** | **AI 回答**。模型生成的文本或数据集中给出的标准答案。            | 在微调训练中，这是唯一需要计算损失（Loss）、让模型学习的部分。 |
## 2. 为什么需要这种格式？
### 2.1 解决“无状态”问题

大模型本身是**无状态**（Stateless）的，它并不记得之前说过什么。 为了实现多轮对话，我们需要手动维护一个包含多个 `role` 和 `content` 字典的列表。每次提问时，将整个列表重新发送给模型，模型通过阅读这些历史记录来“找回记忆”。

### 2.2 统一不同模型的“接头暗号”

虽然我们在代码里写的是通用的 `{"role": "user", ...}`，但不同模型底层的特殊 Token 是完全不同的：

- **Qwen 系列**：使用 `<|im_start|>user\n...<|im_end|>`。
- **Llama 系列**：使用 `<|start_header_id|>user<|end_header_id|>\n\n...`。

通过这种标准的 `role/content` 格式，配合 `tokenizer.apply_chat_template()` 函数，开发者无需为每个模型手动编写复杂的特殊字符，工具会自动完成转换。

### 3. 在微调数据集中的应用

在准备微调所需的 **JSONL** 数据集时，每一行数据本质上就是一个这样的列表。
**示例数据结构**：
```json
[
  {"role": "system", "content": "你是一个法律助手。"},
  {"role": "user", "content": "什么是合同法？"},
  {"role": "assistant", "content": "合同法是调整民事主体之间..."}
]
```
### 工程预警：Labels 掩码（Masking）

在微调（SFT）过程中，我们虽然把整个列表喂给模型，但会利用 `role` 字段进行逻辑区分：

- 我们将 `system` 和 `user` 部分的标签（Labels）设为 **-100**，告诉 PyTorch 忽略这部分的梯度计算。
- 模型只需要预测 `assistant` 角色后的内容，并计算其损失。


