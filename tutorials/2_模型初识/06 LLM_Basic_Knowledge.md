
# 分词器：文本的数字化映射器

## 一、 核心定义：什么是 Tokenizer？

大模型的本质是一个巨大的**矩阵乘法器**，它只能处理**数字（张量）**，而无法直接读懂“你好”或“Hello”这种字符。

**分词器 (Tokenizer)** 的工程定义是：一个负责将**离散的文本字符串**映射为**连续的整数序列**的独立组件。

- **物理位置**：它通常运行在 **CPU** 上，属于数据预处理阶段，不占用昂贵的 GPU 显存。
- **核心逻辑**：它并不理解词义，只是根据一份预先定义好的“密码本”进行查表替换。


在学习大模型时，你可能会觉得 **Token**、**Vocabulary** 这些词很陌生。其实，它们是为了解决计算机“不识字”而演化出来的工程方案。

### 1. 为什么叫 "Token" 而不是 "Word"？

在早期的 NLP 中，我们确实直接使用“单词（Word）”作为单位。但这种做法在工程上面临两个致命问题：

1. **词表爆炸**：如果你把“run”、“runs”、“running”都看作独立的单词，词表会变得无限大，导致显存被撑爆。
2. **未知词（OOV）问题**：一旦用户输入一个字典里没有的新词（如某个新科技名词），模型就会彻底“抓瞎”。

**工程解决方案：子词（Subword）切分** 现代大模型（如 Qwen, Llama）使用的 **Token** 实际上是“子词”。

- **例子**：`"unhappiness"` 可能会被切分为 `["un", "happi", "ness"]`。
- **好处**：模型只需要记住几万个基础的“乐高零件（Subwords）”，就可以拼凑出世界上所有的单词，既节省了显存，又解决了不认识新词的问题。

### 2. 词表 (Vocabulary) 的本质是什么？

你可以把 **词表 (Vocab)** 想象成一本 **“电子字典”**。

- **它的来源**：在模型正式开始训练之前，科学家会收集数万亿字的文本，统计哪些字、词、符号出现频率最高，然后把最常用的前 N 个（Qwen2.5 约为 15 万个）保留下来，并给它们排上号。
- **它的地位**：词表是**静态**的。一旦模型训练完成，这本字典就固定了。
- **它的影响**：词表的大小直接决定了模型 **Embedding 层（嵌入层）** 的物理尺寸。

    - **公式**：$Embedding\ 显存 = 词表大小 \times 隐藏层维度$。        
    - 这就是为什么有些模型词表很大（支持多语言），但对显存的要求也更高。


### 3. 特殊 Token (Special Tokens) 的政治任务

在大模型文件夹中，你会看到 `special_tokens_map.json`。这些特殊 Token 不是为了表达意思，而是为了**给模型下达指令**：
#### 3.1 结构性 Token：控制生成的起止

这类 Token 告诉模型一句话或一个段落的边界。
- **`<|endoftext|>` 或 `</s>` (EOS, End Of Sentence)**：
    - **工程职责**：这是最重要的信号。当模型在生成过程中输出了这个 Token，推理引擎（如 Transformers 或 vLLM）就会立即停止生成。
    - **如果没有它**：模型会陷入“复读机”模式，直到达到最大长度限制为止。
- **`<s>` (BOS, Beginning Of Sentence)**： 
    - **工程职责**：标记序列的开始，提示模型准备接收新的输入。

#### 3.2 占位性 Token：为了矩阵对齐 (Padding)

- **`<pad>` (Padding Token)**：
    - **工程职责**：在批量处理（Batch Processing）时，将短句子补齐到与长句子相同的长度。
    - **物理特性**：它必须配合 **Attention Mask** 使用。在掩码矩阵中，`<pad>` 对应的位置会被标记为 `0`，从而在数学计算中被彻底忽略，不产生任何影响力。
#### 3.3 角色性 Token：定义对话模板 (Chat Template)
对于像 `Qwen2.5-Instruct` 这样的对话模型，它们需要区分“谁在说话”。
- **`<|im_start|>` 和 `<|im_end|>`**：
    - **工程职责**：这是 ChatML 格式的标志。它们像括号一样把 `system`（系统提示词）、`user`（用户问题）和 `assistant`（模型回答）包裹起来。
    - **例子**：`<|im_start|>user\n你好<|im_end|>`。

## 二、 Tokenizer 的工程流程

下面我们完整看一下，分词这个过程做了什么。
当你输入一段话给模型时，Tokenizer 会经历以下三个物理步骤：
#### 1.第一步：切分与编号 (Tokenization & Mapping)

假设我们有两句简单的对话，模型词表（Vocab）中部分单词的编号如下：

- `"Hello"` $\rightarrow$ `101`
- `"!"` $\rightarrow$ `5`
- `"AI"` $\rightarrow$ `202`

当我们处理这两句话时：

- **句子 A**: `"Hello!"` $\rightarrow$ 分词后：`["Hello", "!"]` $\rightarrow$ 编号后：`[101, 5]`
- **句子 B**: `"Hello AI!"` $\rightarrow$ 分词后：`["Hello", "AI", "!"]` $\rightarrow$ 编号后：`[101, 202, 5]`

**此时的问题**：句子 A 长度为 2，句子 B 长度为 3。但在计算时，GPU 要求输入必须是一个矩形阵列（Tensor），不能是这种“长短不一”的列表。

---

#### 2. 第二步：填充 (Padding) —— 对齐长度

为了解决长度不一的问题，我们需要引入 **Padding（填充）**。我们会设定一个“最大长度”（Max Length），并用一个特殊的 ID（通常是 `0`）把短句子补齐。

假设我们将最大长度设为 **4**，并加入特殊的**开始符 (`<s>`)** 和 **结束符 (`</s>`)**：

- **句子 A (处理后)**: `[<s>, 101, 5, </s>, <pad>]` $\rightarrow$ 实际 ID：`[1, 101, 5, 2, 0]`
- **句子 B (处理后)**: `[<s>, 101, 202, 5, </s>]` $\rightarrow$ 实际 ID：`[1, 101, 202, 5, 2]`

> **工程提示**：Padding 虽然解决了对齐问题，但也带来了“无效计算”。如果一个 Batch 里大部分是短句，只有一个超长句，那么短句后面会跟着大量的 `<pad>`，这会浪费 GPU 显存和算力。

---

#### 3. 第三步：掩码 (Attention Mask) —— 屏蔽干扰

模型在计算注意力（Attention）时，会观察序列中的每一个 Token。如果不加干预，模型会去研究那些 `0`（Padding）代表了什么意义。

为了防止模型被“无意义的填充物”误导，我们需要一张**掩码表 (Attention Mask)**：

- **1**：代表这个位置是真实有效的 Token，模型应该关注它。
- **0**：代表这个位置是 Padding，模型在计算时应该完全忽略它。

|**句子**|**Input IDs (经过 Padding)**|**Attention Mask**|
|---|---|---|
|**句子 A**|`[1, 101, 5, 2, 0]`|`[1, 1, 1, 1, 0]`|
|**句子 B**|`[1, 101, 202, 5, 2]`|`[1, 1, 1, 1, 1]`|

####  4. 总结

让我们串联起这个过程。当你输入 `"Hello!"` 给一个 Qwen 模型时，它在后台经历了：

1. **原始输入**: `"Hello!"`
2. **添加特殊字符**: `<|im_start|>user\nHello!<|im_end|>\n<|im_start|>assistant\n`（这是对话模板添加的特殊 Token）。
3. **转为 ID**: `[151644, 872, 198, 14990, 0, 151645, 198, 151644, 77091]`。
4. **Padding & Masking**: 如果长度不够，补 `0`，并生成对应的 `0/1` 掩码矩阵。
5. **送入 GPU**: 最终形成的张量（Tensor）正式进入 Embedding 层。



# 嵌入层 (Embedding)：给数字赋予“空间意义”

在工程中，单纯的数字 ID（如 `101` 和 `102`）是无法进行数学运算的，因为数字的大小并不代表意义的远近。**嵌入层**的任务就是将这些孤立的整数，转换成包含语义信息的**稠密向量（Dense Vector）**。

## 一、定义
### 1. 什么是 Embedding？

**嵌入层**本质上是一个巨大的**查找表（Lookup Table）**。

- **物理构成**：它是一个形状为 `[词表大小, 隐藏层维度]` 的矩阵。
- **工程逻辑**：当一个 ID（如 `101`）进入该层时，它会像拨动开关一样，提取出矩阵中对应的第 `101` 行向量。
- **技术性映射**：这一行向量就是这个 Token 在 AI 世界里的“几何坐标”。

其中在查看模型配置（`config.json`）时，你会看到这两个参数，它们共同决定了 Embedding 层的显存占用：

| **参数名称**          | **工程含义**            | **典型值 (Qwen2.5-1.5B)** |
| ----------------- | ------------------- | ---------------------- |
| **`vocab_size`**  | 词表里总共有多少个不同的 Token。 | 151,936                |
| **`hidden_size`** | 每个 Token 被表示成的向量长度。 | 1,536                  |

### 2. 为什么要进行高维映射？

在 Python 基础中，我们知道 `1 + 1 = 2`。但在自然语言中，我们希望“猫”和“狗”的向量在空间上靠得很近，而“猫”和“手机”靠得很远。
1. **特征表达**：通过将一个 Token 映射为数千维的向量（如 Qwen2.5-1.5B 的隐藏层维度是 1536），模型可以同时记录这个词的语法、语义、情感等多种特征。
2. **可计算性**：变成向量后，模型就可以利用 GPU 进行高效的**矩阵乘法**，计算词与词之间的“相似度”。

**物理映射公式**：

$$\text{Embedding 显存占用} = \text{vocab\_size} \times \text{hidden\_size} \times \text{每个参数的字节数}$$

*注：对于 1.5B 模型，单是这一层通常就要占用约 450MB 显存（按 BF16 精度计算）。*

### 3. 位置编码 (Positional Encoding)：赋予顺序感

这里有一个容易被忽视的工程细节：Transformer 模型本身是**并行处理**所有 Token 的，它分不清“我爱他”和“他爱我”的区别。

为了让模型知道每个词在句子里的**位置**，工程上会在 Embedding 向量中注入一套**位置信号**。

- **物理操作**：将“内容向量”与“位置向量”相加或相乘。
- **结果**：同一个词“你好”，出现在句子开头和句端，其进入下一层时的向量是不一样的。

到目前为止，你的输入数据已经发生了如下物理变化：
1. **原始文本**：`"你好"`
2. **Tokenizer**：变为 ID `[108386, 13]`。
3. **Embedding Layer**：变为两个长度为 1536 的浮点数向量。

## 二、Embedding的工作流程

假设经过 Tokenizer 处理后，我们的输入如下：

- **原始文本**：`"你好，AI"`
- **Input IDs**：`[108386, 13, 202, 0]` (假设 `0` 是 Padding)
- **Attention Mask**：`[1, 1, 1, 0]`
### 第一步：内容映射 (Token Embedding Lookup)

当这组 Input ID 进入 GPU 后，Embedding 层会进行“查表”操作。
1. **物理动作**：GPU 会根据 ID 索引，从预训练好的**权重矩阵**（形状为 `[151936, 1536]`）中提取对应的行。
2. **数据变换**：
    - ID `108386` ("你好") $\rightarrow$ 提取矩阵第 108386 行，变成一个长度为 1536 的浮点数向量。
    - ID `13` ("，") $\rightarrow$ 提取第 13 行向量。
    - ID `202` ("AI") $\rightarrow$ 提取第 202 行向量。
    - ID `0` ("`<pad>`") $\rightarrow$ 提取第 0 行向量。        

**此时的状态**：我们得到了一个形状为 `[4, 1536]` 的张量。但注意，此时“你好”在第一位还是第三位，提取出的内容向量是**完全一样**的。模型还分不清词序。(这里的原因要到后续才能讲清楚，也就是并行运算的部分，不过不影响理解)

---

### 第二步：位置编码 (Positional Encoding / RoPE)

为了让模型知道“你好”在“AI”之前，我们需要给这些向量注入“位置信息”。

1. **生成位置向量**：模型会根据 Token 的索引（0, 1, 2, 3）生成一组特定规律的数字（通常使用正余弦函数或旋转位置编码 RoPE）。
    
2. **叠加运算**：将第一步提取的“内容向量”与“位置向量”进行数学融合（相加或旋转相乘）。
    - `向量("你好") + 位置信号(0)`
    - `向量("AI") + 位置信号(2)`
3. **工程结果**：经过这一步，每个 Token 的向量都带上了唯一的“时空标签”。即使是同一个词，在句子不同位置的**最终向量（Hidden States）**也会不同。

---

### 第三步：掩码应用 (Masking Preliminary)

虽然我们在 Embedding 层已经为所有 Token（包括 Padding）生成了向量，但 **Attention Mask** 就像一个“影子探测器”一直跟在后面。

- **数据流**：此时输出的张量形状依然是 `[4, 1536]`。
- **伏笔**：这张 `[1, 1, 1, 0]` 的掩码表将被传送到下一个环节——**Self-Attention（自注意力层）**。它会在那里告诉模型：“在计算关系时，请无视第 4 个向量，因为它只是个占位符。”

---

### 📊 总结：数据在这一层的变化

| **阶段**  | **数据形态**               | **物理存储占用 (BF16)**                        | **备注**   |
| ------- | ---------------------- | ---------------------------------------- | -------- |
| **进入前** | `[4]` (整数 ID)          | 约 8 字节                                   | 极小       |
| **映射后** | `[4, 1536]` (浮点张量)     | $4 \times 1536 \times 2 = 12.2\text{KB}$ | 膨胀了上千倍   |
| **离开后** | `[4, 1536]` (带位置信息的张量) | $12.2\text{KB}$                          | 准备进入计算核心 |
这里的形态没有算上batch，如果算上了应该是`[batch_size, 4, 1536]`

# Decoder Block：多道工序组成的加工流水线

在工程上，我们不需要拆开每一个数学公式，而是要理解数据进入这个“车间”后经历了怎样的**质量提升**。

### 1. 定义 

**Decoder Block** 是大模型最核心的重复单元。

- **物理结构**：模型（如 Qwen2.5-1.5B）由 **28 层** 完全相同的车间垂直堆叠而成。
- **加工本质**：每一层都接收上一层的向量，经过内部工序处理后，输出一个**语义更丰富**的等尺寸向量。

### 2. 内部的两道核心工序

虽然我们将它视为黑盒，但为了理解显存占用，你需要知道里面有两道主要的“工序”：
#### 工序 A：Self-Attention (自注意力) —— 建立关联

- **功能**：负责让每个词“向左看、向右看”，寻找上下文的联系。
- **输出**：它不会改变数据的形状，但会改变向量里的数值。 已“**苹果** 很好吃，**它** 很红”这个句子为例，让“它”这个向量里包含更多关于“苹果”的信息。
#### 工序 B：FFN (前馈神经网络) —— 提取知识

- **功能**：这是模型的“知识库”。它对融合后的向量进行二次加工，提取出深层的语义特征。
- **工程事实**：FFN 通常占据了一个 Block 中 **2/3 的参数量**。也就是说，模型的“博学”程度主要由这一层决定。

### 3. 输入与输出的“等尺寸”原则

这是初学者最容易掌握的工程规律：**输入是什么形状，输出就是什么形状。**

- **输入 (Input)**：`[Batch, Seq_Len, Hidden_Dim]`    
- **加工**：经过 28 层流水线的反复打磨。
- **输出 (Output)**：`[Batch, Seq_Len, Hidden_Dim]`

# 输出层 (LM Head) —— 从向量回到文字

当数据经过 28 层 Decoder Block 的深度加工后，它依然是高维向量（Hidden States）。要把这些向量变回我们看得懂的文字，需要最后一道“投影”工序。

### 1. LM Head：概率投影仪

**LM Head**（Language Modeling Head）本质上是一个简单的线性层（Linear Layer）。

- **工序内容**：它将长度为 `Hidden_Dim` (1536) 的向量，投影回长度为 `Vocab_Size` (151936) 的超长向量。
- **物理意义**：这个超长向量里的每一个数字，都代表词表里对应位置那个词出现的“可能性”。

### 2. Softmax 与采样：做选择题

投影出来的数字很大且杂乱，我们需要通过 **Softmax** 运算将它们转化为总和为 **100%** 的概率分布。
- **例子**：
    - `"你好"` $\rightarrow$ 90%
    - `"再见"` $\rightarrow$ 5%
    - ... (其它 15 万个词平分剩余概率)

### 3. 解码：逆向查表

最后，模型根据设定的策略（如选择概率最大的，或随机抽样）选出一个 ID（如 `108386`），再由 **Tokenizer 执行逆向查表**，将 ID 变回文字 `"你好"`。


## 二、解码的工作流程

我们继续以输入 **“你好，AI”** 为例，看看在模型预测“下一个字”时发生了什么。
#### 1. 输入状态 (Final Hidden State)

假设数据刚从第 28 层 Decoder Block 出来，最后一个 Token ("AI") 对应的向量是一个长度为 1536 的张量。

- **形状**：`[1, 1536]`

#### 2. 第一步：线性投影 (Projection)

这个向量进入 LM Head 矩阵进行乘法运算。

- **动作**：将 1536 维投影到 151,936 维（词表大小）。
- **输出 (Logits)**：得到一个超长数组，里面有 15 万个数字，每个数字代表对应词的“得分”。
    - 例如：ID 为 520 的词得分 12.5，ID 为 88 的词得分 -2.1。

#### 3. 第二步：Softmax 归一化

将得分转化为概率分布。
- **计算后**：
    - ID 520 ("是") $\rightarrow$ **0.85 (85%)**
    - ID 1024 ("吗") $\rightarrow$ **0.08 (8%)**
    - ID 13 ("，") $\rightarrow$ **0.01 (1%)**
    - ... (其余词平分剩余概率)

#### 4. 第三步：采样与解码 (Sampling & Decoding)

模型根据概率选出一个最合适的 ID。

- **选择**：假设我们选择概率最大的 `520`。    
- **逆向查表**：由 Tokenizer 将 ID `520` 转换为字符串 `"是"`。

#### 5. 循环往复 (Autoregressive)

- **物理动作**：将生成的 `"是"` 拼接到原来的输入后面，变成 `"你好，AI是"`，重新喂给模型。
- **结果**：模型开始预测下一个词。这就是为什么大模型生成文字是一个字一个字跳出来的原因。


